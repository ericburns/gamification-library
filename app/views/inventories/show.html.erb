<p id="notice"><%= notice %></p>

<table class="table table-striped">
	<tr>
		<th>User</th>
		<th>Badge</th>
		<th>Amount</th>
	</tr>
	<tr>
		<td><%= @user.username %></td>
		<td><%= @game.badges.find(@inventory.badge_id).badge_name %></td>
		<td><%= @inventory.amount %></td>
	</tr>
</table>

<%= link_to 'Edit', edit_game_user_inventory_path(@game, @user, @inventory) %> |
<%= link_to 'Back', game_user_inventories_path %>